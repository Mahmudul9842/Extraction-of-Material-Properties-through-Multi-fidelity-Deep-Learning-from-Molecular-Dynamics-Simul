Run the combine.m file in MATLAB to train the neural network. (edit the directory name at line 239)

Then run the data.m in MATLAB to get the trained data in data.txt file

The first column of data.txt is temperature, second column is volume fraction and the third column is viscosity prediction of MPINN